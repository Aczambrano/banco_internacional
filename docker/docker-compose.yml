version: "3.8"

services:
  wiremock:
    image: wiremock/wiremock:3.5.4
    container_name: wiremock-client
    ports:
      - "8089:8080"
    volumes:
      - ./wiremock:/home/wiremock
    command: "--verbose"